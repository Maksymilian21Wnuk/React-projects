type Field = {
    htmlfor : string,
    label : string,
    type : string
  }

type Button_props = {
    button_vals : string
};

type Basic_label = {
    htmlfor : string,
    label : string
}

type Header = {
    text : string
}

type Radio = {
    htmlfor : string,
    label : string,
    values : string[]
}

type SelectType = {
    htmlfor : string,
    label : string,
    values : string[]
}